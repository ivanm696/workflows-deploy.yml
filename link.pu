import os
import datetime

def find_epochs(folder="."):
    """Находит все свитки эпох (файлы с 'epoch_' в названии)"""
    return sorted([f for f in os.listdir(folder) if f.startswith("epoch_") and f.endswith(".md")])

def link_epochs(folder=".", output="link.md"):
    """Создаёт свиток-связь между эпохами"""
    epochs = find_epochs(folder)
    timestamp = datetime.datetime.now().strftime('%Y-%m-%d %H:%M')
    with open(output, "w", encoding="utf
